{
  "license": "Apache-2.0",
  "scripts": {
      "postinstall": "install-app-deps",
      "pack": "build",
      "dist": "build"
  },
  "builder": {
    "osx": {
        "title": "IRCCloud"
    }
  },
  "devDependencies": {
    "electron-builder": "^2.8.3",
    "electron-prebuilt": "^0.36.9",
    "rimraf": "^2.5.2",
    "tsconfig-glob": "^0.4.1",
    "typescript": "^1.8.2",
    "jshint": "^2.9.1"
  }
}
